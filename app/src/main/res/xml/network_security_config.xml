<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for NetGuard Pro
    This restricts cleartext traffic and configures certificate pinning for our backend.
    See: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!-- Prevent cleartext traffic to all hosts -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds may allow cleartext for local testing servers -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
